import 'package:flutter/material.dart';

class IconManager {
  static const Icon navRight = Icon(Icons.navigate_next);
  static const Icon navLeft = Icon(Icons.navigate_before);
  static const Icon hollowCircle = Icon(Icons.circle_outlined);
  static const Icon solidCircle = Icon(Icons.circle_rounded);
}
